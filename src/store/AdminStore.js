const state = {
  state: [12, 2, 32],
  clubs: [],
};

const getters = {};

const actions = {
  //! events managment
  //! clubs managment
};

const mutations = {};

export default {
  namespace: true,
  state,
  getters,
  actions,
  mutations,
};
